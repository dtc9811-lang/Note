# 改好的稿子放这里

文件名必须和 `分段/` 里的一致，例如：

```
分段/01_ch001-004.md   →   修改/01_ch001-004.md
分段/02_ch005-012.md   →   修改/02_ch005-012.md
```

没放进来的，合并时会自动用 `分段/` 里的原文补上，所以可以分批改、随时合并。

合并：
```bash
cd "F:/novel/百世飞升续写"
python 分段/合并脚本.py            # 预览
python 分段/合并脚本.py --write    # 写出 1-105.改后.txt
```
