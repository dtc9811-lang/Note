# AGENTS.md —— 给 AI 的入口

> 任何新会话，**先读这一页**。它告诉你：东西在哪、哪些能改、怎么改、有什么坑。
> 人看的说明在 `README.md`。

项目：**《百世飞升》（白眉罗汉，已完本）同人续写 · 第十世**。
工作区 = 本目录，同时也是 git 仓库（remote: `https://github.com/dtc9811-lang/Note.git`）。

---

## 〇、开工前：先接 DevTrace（本机统一工程记录系统）

DevTrace 已配为 MCP 服务器 `devtrace`（全局配置 `~/.config/mcp/mcp.json`，HTTP，非 stdio）。
工具：`resolve_workspace`、`search_tasks`、`get_task_context`、`create_task`、`update_task`、`record_progress`。

**开工第一步，不用问人：**

1. `resolve_workspace(cwd)` —— cwd 传**当前工程的真实工作目录**
2. `matched=true` → 选最相关的 active Task → `get_task_context(task_id)` → 再动手
3. 找不到明确 Task → `search_tasks`；**确认确实没有**才 `create_task`
4. 完成有意义的修改／测试／失败实验／发现／决策／风险 → `record_progress`

**任务状态要跟着维护**：阶段完成后同步 `status` / `next_step` / `current_conclusion` / `current_risk`。
`next_step` 当前可执行时**直接执行**，不要只复述给用户。

**收尾口径**：

- 目标完全完成 → `status="done"`、`next_step=""`
- 仍需实机验证／用户提供数据／外部条件／硬件测试 → 用 `verify` 或 `blocked`，并写清 `next_step`

**字段**：`agent` 填实际模型名（如 `deepseek-v4-flash`），`source` 固定 `pi`，
`display_summary` 写成人能在 Web 页面一眼扫读的一句话。

---

## 一、最重要的一件事：谁是真源

```
百世飞升续写/分章/第001章_….md … 第114章_….md   ← ★ 唯一真源。改正文只改这里
百世飞升续写/全本.txt                            ← 生成物。由脚本合并而成，不要手改
```

**`全本.txt` 会被脚本覆盖。** 直接改它 = 改动丢失。

改完正文必须让两者同步：

```bash
cd F:/novel/百世飞升续写
python 脚本/正文管理.py 全本      # 重新生成 全本.txt
python 脚本/正文管理.py 检查      # 确认全本与分章逐章一致
```

---

## 二、命令速查

```bash
cd F:/novel/百世飞升续写

python 脚本/正文管理.py 状态              # 所有章：章号、章名、字数
python 脚本/正文管理.py 检查              # 校验章号连续性 + 全本与分章一致性
python 脚本/正文管理.py 全本              # 由 分章/ 生成 全本.txt（加 --干净 去掉提示头）
python 脚本/正文管理.py 重编号            # 按文件名顺序重写各章首行与文件名
python 脚本/正文管理.py 插入 <N> <文件>    # 在第 N 章之后插入，后面自动顺延
python 脚本/正文管理.py 追加 <文件>        # 加为最后一章
python 脚本/正文管理.py 删除 <N>          # 删除第 N 章，后面自动补位
python 脚本/正文管理.py 替换 <N> <文件>    # 用文件内容替换第 N 章

python 脚本/文风体检.py                  # 全书体检（只报有问题的）
python 脚本/文风体检.py 37               # 只看第 37 章
python 脚本/文风体检.py 读感             # 读感统计（章长/场数/对话/世界·修炼·战斗词密度）
python 脚本/文风体检.py <文件路径>        # 体检任意草稿
```

**增删章一律走 `插入`/`删除`/`替换`**——它们会自动重编号 + 重生成全本，不要手工改文件名。

提交：

```bash
cd /f/novel && git add -A && git commit -m "..." && git push
```
（代理已配在 `.git/config`，需要 Clash 在 127.0.0.1:7897。国内直连 GitHub 会断。）

---

## 三、目录

```
F:\novel\
├── AGENTS.md               ← 本页
├── README.md               ← 给人看的
└── 百世飞升续写\
    ├── 全本.txt              ★ 生成物，别手改
    ├── 分章\                 ★ 真源，114 章
    ├── 脚本\
    │   ├── 正文管理.py        增删改章 / 生成全本 / 校验
    │   └── 文风体检.py        硬指标体检（「的，」密度、逗号、引号、套话、术语）
    ├── 千问写作包\           写作工具包（入口 README.md；总览与优化 27 号文）
    ├── 旧文档\               早期设定/章纲，**只作参考、不作依据**（原著探索笔记、
    │                         共创设定记录、篇幅与档位 v2、高光点子库、高潮与模型档位
    │                         编排、71-105章纲、改稿期交接卡、审查修正）
    ├── qwen新写_前30章重写\   前30章重写草案（**已并入正本 ch3—29**，留档参考）
    ├── 修改\06_ch033-040.md  正本 39—47 重写版（**未合并**，见 24 号文第 6 节）
    └── 补写\重写_ch08_监墟使.md  已作废（正本 ch8 现为「坊市」），留档仅备比照
```

---

## 四、当前状态

| 项 | 值 |
|---|---|
| 正本 | 114 章，**162569 正文字**（以 `正文管理.py 状态` 为准，别再往文档里写死） |
| 章号连续 | 是，1—114 无缺口 |
| qwen 草案复审清理 | **已做**（2026-09 批次二）：卫青→卫老修士、ch24—29 顺序校正、章末模板重写、长照渡→长照；体检脚本新增跨章重复+自造人名检测（见台账第十节）|
| 前30章重写 | **已并入**（qwen 草案，ch3—29），原 111 章 → 115 章 → **去重后 114 章** |
| 前20章去重 | **已做**（2026-09，台账第十三节）：删冗余章 ch5「劫贡」（全文被 ch6「点卯」覆盖）+ ch3/4 逐字去重 + 「再撑一百年」套路从 165 处降到 3 处。**副作用：所有 ≥5 的正本号前移 1** |
| 仙体醒度 | 三成（现第 114 章「点灯」） |
| 洞天尺度 | 四里；仙炁零缕，尚不能自产 |
| 世界线回填 | 23 号文 B0—B3＋附 **全部完成**，qwen 草案复审清理已完成（台账第十节）；**下一步 116 起续写，执行稿见 `千问写作包\24_续写执行规划_116-150.md`** |
| 前105章优化 | 25 号文 **P0—P2 完成**（台账第十一节）：全书硬伤 46→0、单章 tics≤3、三条底线补场 11 处；P3 暂缓；两项待裁定均已结（世界三栏口径已拍板；ch25/28 卖图雷同已差异化） |
| 全书总览与优化 | 27 号文（2026-09）：**原最大短板 31—70 章密度已补完**——三轮 40 章 29960→**45011** 字、均 749→**1125**、<800 字章 28→0（台账第十二节） |
| 去重后待补密度 | ch3 身世 **1184** 字、ch4 旧例 **1396** 字、ch11 慢半日 **1004** 字（删重复后不足 1500 目标线），需补密度 |
| 下一步 | ① ~~31—70 补密度~~ ✅ 已完成 → ①′ 给 ch3/4/11 三章补密度回 1500+ → ② 生成 125—126 章卡＋把大椿界落地写进 123 章卡（`24` §5／§9.4）（原 126—127／124，删章后 −1）→ ③ **开写 115**（`13_正本116完整提示词.md`＋`15_第115章交接卡_正式版.md`；两文件名待 −1 重命名） |

**已并入的前30章重写**（源草案留在 `qwen新写_前30章重写\`，只作参考）

- 新插入 4 章：身世(3)、家什(6)、坊市(7)、仙籍(27)
- 替换 18 章：旧例(4)、点卯(5)、入墟(8)、坐标物(10)、慢半日(11)、潮倒卷(12)、
  死门(13)、另一层(14)、半步(15)、清场(16)、旧例还认(18)、缴清(20)、一成(21)、
  无归(22)、先医后问(23)、废物(24)、棺船(25)、窦让(28)
- **原封未动**：回声(9)、谁带路(17)、印与账(19)、改姓派(26)
- **已删**：劫贡（原 5）——全文被点卯覆盖，2026-09 去重时删除；原编号已整体 −1
  —— 草案没覆盖，现挂在新区块里，序可能不顺，需要时再调

**未合并进正本的东西（改动前先问人）**

1. `修改\06_ch033-040.md` —— 旧号 33—40 重写版（= 正本 **39—47**，对位表见文件头）。引号已转全角；合并时按内容对位，不要按旧章号。
2. `补写\重写_ch08_监墟使.md` —— **已作废**。正本 ch8 现为「坊市」，监墟使内容已拆进 ch6/ch11。留档仅备比照。

---

## 五、坑（务必先看）

### 1. 千问写作包里有一套「旧章号」

`12_卷纲_106-140.md`、`章卡\batch1_第106-115章.md` 用的是**最早旧编号**（`13` 已换算成正本号并改名 `13_正本116完整提示词.md`，`15` 同理 → `15_第115章交接卡_正式版.md`）。已三轮顺延（+6 补写、+4 前30章重写、**−1 删 ch5 冗余章**）。
换算：**正本号 = 旧章号 + 9**（2026-09 删 ch5 后由 +10 变 +9）。

| 写作包里写的 | 实际是正本的第几章 |
|---|---|
| 106（四成·开府） | **115** |
| 115（四成半） | **124** |
| 140 | **149** |

⚠ **删 ch5 的连带影响（待清）**：所有 ≥5 的正本号整体前移 1。
`13_正本116完整提示词.md`、`15_第115章交接卡_正式版.md` 的文件名，
`09`/`10`/`11`/`12`/`16`—`20` 的 banner，`章卡\batch1_第106-115章.md`，
以及 `24` 号文的 116—150 区间，均需按 −1 重命名／改号。

**带旧号的文件还有**（都已加 banner，引用前先看 banner）：
`09`、`10`、`11`、`12`、`16`—`20`、`提示词\生成章卡.md`、`章卡\batch1_第106-115章.md`。
已换算为正本号并改名的两个：`13_正本116完整提示词.md`、`15_第115章交接卡_正式版.md`。

世界层次与换地图排布以 `21_世界层次阶梯与改纲.md` 为准（界域／路网／仙籍三阶），**不在这套旧号换算里**。

### 2. 引号：正文统一全角 `“ ”`

**已全部清理完毕**：原补写章（现第 29、45、56、69、90 章）与重写章 114「点灯」的半角引号已全部转为全角，正文不再有半角引号（新写时仍须注意）。

同批已清理：正文中 19 处 `chNN` 元叙述（改为剧情内指称）、ch14 英文残留「dust」、ch91 一处引用未来章的时间线 bug。详见 `千问写作包/14_写作进度台账.md` 第八节。

### 3. 老章普遍不满足新铁律

`文风体检.py` 全书跑会报一大片「的，」密度超 4/千字。**这是历史遗留，不是要你全改。**
铁律是**新写/重写的章**才必须达标。

已并入的 qwen 草案里原不达标的 8 章（旧例、点卯、坊市、入墟、死门、另一层、一成、无归）
**已全部修复达标**（2026-09 批次，只改句式不动情节，详见台账第八节）。

### 4. 不要自造名字

没在章卡里出现名字的人，一律不具名（"一个散修""渡口的帮手"）。
新地名不给名字。详见 `千问写作包\07_禁区与自检清单.md` G 节。

### 5. 规划文档的任务单不能盲信，落笔前先对正文

2026-09 回填批次实踩（详细清单见 `千问写作包\14_写作进度台账.md` 第九节）：

- **章号对不上内容**：23 号文写"ch50《接在哪里》古驿场"，古驿实在 **ch51《古驿》**。一律按内容对位，不按章号。
- **道具时代错**：任务单让 ch37/38"把残片按在残图上"，但残图 **ch75 才出现**。回填前先 grep 道具首现章；当时改用赵升自绘草图，ch75 得残图后不冲突。
- **世界阶口径打架**：21 号文排布表与 23 号文任务单对 ch114 时点的路网/仙籍阶说法不一（已在台账第四节标 ⚠ 待人工裁定）。写正文不引数字，只呈现现象，可绕开这类矛盾。
- **插入前查上下文道具状态**：ch64 文书已"拿到手里"，初稿又写"接过来"，穿帮。锚点句要逐字核对（"他没有说话"≠"赵升没有说话"）。

### 6. 工具层的坑（Windows / Git Bash）

- **grep 统计中文不准**：`grep -o "的，" | wc -l` 对多字节串会多数（实测 45 vs 真实 7）。**计数一律用 python**。
- **控制台中文乱码**：python 输出中文前加 `PYTHONIOENCODING=utf-8`，或重定向到文件再读。
- **git push 偶发失败**：代理（Clash 127.0.0.1:7897）不稳，直接重试即可；commit 不会丢。
- **新写正文易错字**：拄/抄、焐/焘、仙炁/仙气。写完必跑 `文风体检.py N` + 自查同音字。

### 7. 改老章文风时的呼应句

「井底是黑的」「灯座是空的」「井壁是凉的」这类句子是**跨章复现的意象**，ch14/15 还有整句镜像。
修「的，」密度时：呼应句尽量保留，改别处；镜像句要改就两章同步改。

---

## 六、写作铁律（写任何正文前必读）

来源：`百世飞升续写\千问写作包\07_禁区与自检清单.md`

**世界观（不可违反）**

1. 仙墟内法力只出不进、消耗加倍。仙墟外（坊市、路网、渡口、洞天）正常。
2. 路网只认功能，不认名字、名分、凭证。做样子也算做。
3. 三条规矩的固定写法：**灯不熄、门不锁、来者先医后问。**
4. 不杀赵玄靖、鸿运子、天运子、石灵。
5. **"归处"一栏，一字不得解释。**
6. 不得使用已废止方案：残灯、石灵护送、千人村、第1001人、记名供灯、五镜副本。
7. 正文里绝不能出现"第X章"这类元叙述。

**文风（硬指标）**

- 「的，」密度 ≤ 4/千字
- 单句逗号 ≤ 6 个
- 章末停在动作或画面上，不抒情、不总结
- 不用套话：嘴角勾起、眸光一凝、气息暴涨、倒吸一口凉气、与此同时、淡淡道、冷笑道……
- 不用模糊数量："许多""无数""很多" → 换具体数字
- 术语不得用同义词替换（`06_固定术语表.md` 有对照表）

**修炼章五段**：决定 → 硬扛 → 成了 → 当场用出来 → 代价。缺一不可。

**三条底线（2026-09 终审，写任何章都要有）**：
1. **大世界联动**：每 5—8 章必须有一次“外面的事影响这里”或“这里的事被上面看见”；每 20—30 章换一次地图。
2. **修炼**：本卷 4 节点（116/125/134/143）写足五段；修炼循环转两圈。
3. **战斗**：每 5—8 章至少一场身体层面的冲突（打斗／天灾／硬扛），可短不能无。
   （主线仍不靠力量解决；战斗只负责证明变强、制造危机、露世界层次。实测 71—114 全段无正面战斗，是教训。）

---

## 七、接着该读什么

| 想知道 | 读 |
|---|---|
| 写到哪、还欠什么、伏笔台账 | `千问写作包\14_写作进度台账.md` |
| 前情摘要 | `千问写作包\11_前情摘要_1-105.md` |
| 人物 / 设定 / 修炼阶梯 | `千问写作包\09_人物速查卡.md`、`08_设定速查卡.md`、`10_修炼阶梯.md` |
| 文风范文与反例 | `千问写作包\05_文风样本.md` |
| 已修正的硬矛盾 | `千问写作包\16_复审报告与修正记录.md` |
| 怎么让千问写新章（四步流程） | `千问写作包\README.md` |
| 旧编号卷纲（换算见第五节） | `千问写作包\12_卷纲_106-140.md` |
| **世界层次/改纲（换地图与抬层）** | `千问写作包\21_世界层次阶梯与改纲.md` |
| **116 起怎么写（分批/验收/模型规则/待拍板）** | `千问写作包\24_续写执行规划_116-150.md` |
| **前 105 章怎么优化（P0 硬伤/P1 tics/P2 底线回填）** | `千问写作包\25_前105章优化方案.md` |
| **全书总览、7 处结构问题、优化排序** | `千问写作包\27_全书总览与优化方案.md` |
| **31—70 还差 1.1 万字、怎么交 qwen 写（逐章任务单＋锚点句）** | `千问写作包\28_31-70补密度任务单_qwen.md` |
| **审校遗留怎么交 qwen 改（校订版提示词）** | `千问写作包\26_审校遗留修复提示词.md` |