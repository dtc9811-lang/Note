# AGENTS.md —— AI 开工入口

项目：**《百世飞升》第十世续写**。工作区 `F:\novel`。

## 1. 开工顺序：先 DevTrace，再文件

DevTrace 是唯一的**活状态控制层**。开始任何写作/规划任务先：

1. `resolve_workspace(cwd)`，cwd 传 `F:\novel` 或当前真实工作目录。
2. 选择最相关 active Task，调用 `get_task_context(task_id)`。
3. 找不到 Task 才 `search_tasks`；确认没有合适 Task 后才 `create_task`。
4. 先读 Task 的 `current_conclusion / current_risk / next_step` 和最近 Events，再读文件。

优先级固定：

```text
分章正文 + 正文管理.py 的实测结果
> DevTrace 当前 Task / decision
> 写作资料中的固定设定
> 长期总纲
> 历史备份
```

如果旧规划和正文冲突：**正文事实优先**。如果旧规划和 DevTrace 当前状态冲突：**DevTrace 优先**。

## 2. 两个模型的固定分工

### DeepSeek Flash：剧情规划 / 验收

负责：
- 读 DevTrace 当前状态和最近正文；
- 每次只做**下一章的正式施工单**，最多顺带预判后两章；
- 检查连续性、设定冲突、节奏和伏笔；
- 规划写入 DevTrace：`event_type=note`；
- Qwen 写完后做快速验收：`event_type=test`。

不要直接大段写正文，不要一次重规划十几章。

### Qwen 3.8 Max：正文执行

负责：
- 读取 DevTrace 最新施工单；
- 按施工单写当前一章，不擅自重规划主线；
- 局部文学发挥可以，但新增事实/物件/伏笔/偏离必须回写；
- 正文完成后跑脚本验收；
- 用 `record_progress(event_type="change")` 写入本章实际结果。

Qwen 不再自行综合一堆旧章卡、旧交接卡、旧任务单。

## 3. 标准闭环

```text
Flash 规划下一章（note）
→ Qwen 写正文（change）
→ 正文管理.py + 文风体检.py
→ Flash 验收（test）
→ 更新 current_conclusion / current_risk / next_step
→ 下一章
```

完成一个剧情批次后，再更新 `写作资料/04_前情与伏笔.md`。**不再每章维护独立交接卡文件。**

DevTrace Event 语义：
- `note`：章节施工单 / 规划
- `change`：正文或改稿完成
- `test`：章节验收
- `decision`：已拍板设定，后续模型不得随意推翻
- `finding`：从正文发现的新事实或连续性问题
- `risk`：当前剧情/连续性风险
- `failed_experiment`：废弃方案或失败试写

## 4. 唯一正文真源

```text
百世飞升续写/分章/第NNN章_….md   ← 唯一正文真源
百世飞升续写/全本.txt            ← 生成物，不手改
```

增删改正文后：

```bash
cd F:/novel/百世飞升续写
python 脚本/正文管理.py 全本
python 脚本/正文管理.py 检查
python 脚本/文风体检.py <章号>
```

查当前真实章数：

```bash
python 脚本/正文管理.py 状态
```

**永远不要从旧文件名推断下一章。**

## 5. 现在只认这 4 份写作资料

```text
百世飞升续写/写作资料/
├─ 01_写作规则.md       写法、禁区、Flash/Qwen流程、验收
├─ 02_设定总表.md       世界观、术语、人物、修炼、法宝
├─ 03_长期总纲.md       当前卷宏观节拍与远期方向
└─ 04_前情与伏笔.md     批次级快照、未回收伏笔、连续性锚点
```

正文前按需取用，不要四份全部整包塞进上下文。

另有：

```text
百世飞升续写/待处理/
```

这里只放**尚未并入正本**、以后需要人工决定的候选稿。它们不是依据。

## 6. 写作硬规则摘要

详细见 `写作资料/01_写作规则.md`。

必须守：
- 仙墟内法力只出不进、消耗加倍；仙墟外正常。
- 路网只认功能，不认名字、名分、凭证；做样子也算做。
- 三条规矩固定写法：**灯不熄、门不锁、来者先医后问。**
- “归处”一字不得解释。
- 不使用废止方案：残灯、石灵护送、千人村、第1001人、记名供灯、五镜副本、私人仙墟、墟主职业、赵氏仙朝。
- 未登记的新地名/势力/具名人物不得直接写入正文。
- 新写/重写章：「的，」≤4/千字；单句逗号≤6；章末停在动作或画面。

## 7. DevTrace 收尾

每轮工作结束前：
- 有意义的规划/正文/验收必须记录 Event；
- `display_summary` 写成一眼能扫懂的一句话；
- `current_conclusion` 只放当前仍成立的正史状态；
- `current_risk` 只放当前仍存在的问题；
- `next_step` 写给下一位模型；
- 下一步当前可执行就直接推进，不只复述。

剧情批次全部完成才 `status=done`。只是本章写完而批次未完，继续保持 `in_progress` / `verify`。
